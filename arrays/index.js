//arrays
let input = ['a', 'b', 'c'];

//accessing elements
console.log(input[0]);

//adding elements
input[3] = 'd';

//removing elements
delete input[2];

//iterating over arrays
for (let i = 0; i < input.length; i++) {
    console.log(input[i]);
}